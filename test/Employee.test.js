const { it } = require("node:test");
const Employee = require("../lib/Employee");


describe("Employee", () => {
    describe("getName", () => {
        it('Returns user inputed ID number', () => {

            expect().toEqual
        })
    })
})